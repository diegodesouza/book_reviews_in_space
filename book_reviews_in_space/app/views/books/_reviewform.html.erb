<%= form_for([@book, @book.reviews.build]) do |f| %>
<div>
  <%= f.label :description %>
  <%= f.text_area :description %>
</div>

<div>
  <%= f.submit "Save" %>

</div>
<% end %>

<%= link_to "Edit", edit_book_review_path(:book_id, :review_id)  %>
