<%= form_for([@book, @book.reviews.build]) do |f| %>
  <div>
    <%= f.label :description, "Description: "%>
    <%= f.text_area :description %>
  </div>

  <div>
    <%= f.submit "Save" %>
  </div>

<% end %>
